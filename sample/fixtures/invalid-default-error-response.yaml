openapi: 3.0.3
info:
  title: Invalid default error response
  version: 1.0.0
paths:
  /responses:
    get:
      operationId: getDefaultError
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        default:
          description: Undocumented generic error body
components:
  schemas:
    StatusResponse:
      type: object
      properties:
        status:
          type: string
