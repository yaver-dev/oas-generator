{{#splitSchemas}}namespace {{schemasPackageName}};{{/splitSchemas}}{{^splitSchemas}}namespace {{packageName}}.{{modelPackage}};{{/splitSchemas}}

/// <summary>
/// Empty payload used to preserve the RPC result envelope for bodyless HTTP responses.
/// </summary>
[global::MessagePack.MessagePackObject(keyAsPropertyName: true)]
public sealed class EmptyResponse
{
    public static EmptyResponse Instance { get; } = new();

    public EmptyResponse()
    {
    }
}
